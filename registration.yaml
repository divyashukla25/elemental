apiVersion: elemental.cattle.io/v1beta1
kind: MachineRegistration
metadata:
  name: efive-nodes
  namespace: fleet-default
spec:
  config:
    cloud-config:
      users:
        - name: root
          passwd: root
    elemental:
      install:
        reboot: true
        device: /dev/sda
        debug: true
  machineName: efive-machine
  machineInventoryLabels:
    location: "efiveeurope"
