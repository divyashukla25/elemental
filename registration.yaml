apiVersion: elemental.cattle.io/v1beta1
kind: MachineRegistration
metadata:
  name: elementt1-nodes
  namespace: fleet-default
spec:
  config:
    cloud-config:
      users:
        - name: root
          passwd: root
    elemental:
      install:
        reboot: true
        device: /dev/sda
        debug: true
  machineName: elementt1-machine
  machineInventoryLabels:
    location: "elementt1europe"
